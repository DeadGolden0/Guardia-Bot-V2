version: '3.8'

services:
  bot:
    build: .
    container_name: guardia-bot
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - MONGO_URI=${MONGO_URI}
    volumes:
      - .:/usr/src/app
    ports:
      - "3009:3009"
    restart: unless-stopped
